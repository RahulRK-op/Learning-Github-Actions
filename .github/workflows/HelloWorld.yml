name: Hello World

on: push

jobs:
  hello-world-job:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - run: echo "$(cat hello_world.txt)"
